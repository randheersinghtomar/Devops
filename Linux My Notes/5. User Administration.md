# Module 5: User Administration

- 1. User Creation
- 2. User Information & Configuration
- 3. Password Management & Policy
- 4. User Modification
- 5. Lock, Unlock & Expiration
- 6. User Deletion
- 7. Login & Home Directory
- 8. UID & Service Accounts
- 9. Troubleshooting & Interview
---
# Module 5: User Administration

## 1. User Creation

- `useradd` / `adduser`
- UID, home directory, shell, comment
- System/service users
- `/etc/skel`

## 2. User Information & Configuration

- `id`, `whoami`, `who`, `w`, `last`
- `/etc/passwd`
- `/etc/shadow`
- `/etc/login.defs`
- `/etc/default/useradd`

## 3. Password Management & Policy

- `passwd`
- Password aging & expiry
- `chage`
- Force password change
- Password complexity/history
- PAM & `pam_pwquality`

## 4. User Modification

- `usermod`
- Change UID / username
- Change or move home directory
- Change login shell
- Modify user information

## 5. Lock, Unlock & Expiration

- Lock / unlock user
- Password vs account expiration
- `passwd -l` / `passwd -u`
- `usermod -L` / `usermod -U`
- Account expiration

## 6. User Deletion

- `userdel`
- `userdel -r`
- Files owned by deleted users
- Safe production deletion

## 7. Login & Home Directory

- Login shells
- `/bin/bash`, `/sbin/nologin`, `/bin/false`
- Home directory management
- SSH login troubleshooting

## 8. UID & Service Accounts

- UID `0`
- Normal vs system users
- UID conflicts
- Service accounts

## 9. Troubleshooting & Interview

- User cannot login
- Password expired/locked
- SSH login failure
- Wrong shell/home directory
- UID/account issues
- Production scenarios
- **4-year interview questions**
---
---

## 1. User Creation

## 1. `useradd` vs `adduser`

### `useradd`

`useradd` is the standard Linux command used to create a user.

### Basic syntax

```bash
sudo useradd username
````

Example:

```bash
sudo useradd lokesh
```

Check the user:

```bash
id lokesh
```

Check the user's entry:

```bash
grep "^lokesh:" /etc/passwd
```

---

### `adduser`

On Debian/Ubuntu systems, `adduser` is a more user-friendly and interactive utility for creating users.

```bash
sudo adduser lokesh
```

It may interactively ask for:

* Password
* Full name
* Other user information

### Interview Point

> `useradd` is the lower-level command, while `adduser` is generally a more interactive/user-friendly utility available on Debian-based systems.

---

# 2. Create a User with a Home Directory

Use `-m` to create the user's home directory.

```bash
sudo useradd -m testuser
```

Check the home directory:

```bash
ls -ld /home/testuser
```

Check the user:

```bash
id testuser
```

Check the `/etc/passwd` entry:

```bash
grep "^testuser:" /etc/passwd
```

### `-m`

```text
-m → Create the user's home directory
```

---

# 3. Specify the Login Shell

Use `-s` to specify the user's login shell.

```bash
sudo useradd -m -s /bin/bash testuser
```

Check:

```bash
grep "^testuser:" /etc/passwd
```

Example:

```text
testuser:x:1001:1001::/home/testuser:/bin/bash
```

The last field is the user's login shell.

Common shells:

```text
/bin/bash
/bin/sh
/sbin/nologin
/bin/false
```

---

# 4. Specify UID

Use `-u` to manually specify the user's UID.

```bash
sudo useradd -u 1500 -m testuser
```

Check:

```bash
id testuser
```

Example:

```text
uid=1500(testuser)
```

### Why specify a UID?

This can be useful during:

* Server migrations
* Application migrations
* Restoring applications
* Maintaining consistent file ownership
* Certain NFS/shared-storage environments

### Interview Question

**Q: What happens if two users have the same UID?**

Linux identifies file ownership using the UID rather than just the username.

Duplicate UIDs can therefore cause ownership and access confusion and should generally be avoided.

---

# 5. Specify User Comment

Use `-c` to specify the user's comment/GECOS field.

```bash
sudo useradd -m -c "Linux Administrator" testuser
```

Check:

```bash
grep "^testuser:" /etc/passwd
```

Example:

```text
testuser:x:1001:1001:Linux Administrator:/home/testuser:/bin/bash
```

The comment/GECOS field can contain information such as:

* Full name
* Employee information
* User description

---

# 6. Combine User Creation Options

In real administration, multiple options are commonly combined.

Example:

```bash
sudo useradd -m -u 1500 -s /bin/bash -c "Test Linux User" testuser
```

### Meaning

```text
-m
→ Create home directory

-u 1500
→ Set UID to 1500

-s /bin/bash
→ Set login shell

-c "Test Linux User"
→ Set comment/GECOS information
```

Verify:

```bash
id testuser
```

```bash
grep "^testuser:" /etc/passwd
```

---

# 7. System / Service Users

A system user is generally created for running services or applications rather than for a human user.

Use `-r`:

```bash
sudo useradd -r nginxuser
```

Check:

```bash
id nginxuser
```

```bash
grep "^nginxuser:" /etc/passwd
```

---

## Service User with No Interactive Login

For a service account that should not have interactive login:

```bash
sudo useradd -r -s /usr/sbin/nologin nginxuser
```

Check:

```bash
grep "^nginxuser:" /etc/passwd
```

Example:

```text
nginxuser:x:998:998::/home/nginxuser:/usr/sbin/nologin
```

The exact UID and home-directory behavior can vary by Linux distribution and configuration.

---

## Why Use Service Accounts?

Applications should generally run using a dedicated account rather than `root`.

Examples:

```text
Nginx       → nginx user
MySQL       → mysql user
Jenkins     → jenkins user
Application → application-specific user
```

This follows the **principle of least privilege**.

### Interview Question

**Q: Why shouldn't applications normally run as root?**

Because if the application is compromised, an attacker could potentially gain root-level control of the system.

Using a dedicated service account limits the privileges available to the application.

---

# 8. `/etc/skel`

`/etc/skel` contains default files that can be copied into a newly created user's home directory.

Check its contents:

```bash
ls -la /etc/skel
```

You may see:

```text
.bashrc
.bash_logout
.profile
```

When you create a user with a home directory:

```bash
sudo useradd -m testuser
```

the files from `/etc/skel` are typically copied into:

```text
/home/testuser/
```

Check:

```bash
ls -la /home/testuser
```

---

## Practical Use of `/etc/skel`

Suppose a company wants every new user's home directory to contain a standard configuration file.

You can place the required default file under:

```text
/etc/skel/
```

When a new user's home directory is created, the file can be copied there automatically.

### Important

`/etc/skel` is a template for **new users**.

Changing files in `/etc/skel` does **not automatically update existing users' home directories**.

---

# 9. Important `useradd` Options

| Option | Purpose                        |
| ------ | ------------------------------ |
| `-m`   | Create home directory          |
| `-d`   | Specify home directory         |
| `-s`   | Specify login shell            |
| `-u`   | Specify UID                    |
| `-c`   | Specify comment/GECOS          |
| `-r`   | Create system user             |
| `-M`   | Do not create home directory   |
| `-e`   | Set account expiration date    |
| `-f`   | Set password inactivity period |

Example:

```bash
sudo useradd -m -u 1500 -s /bin/bash -c "Linux User" testuser
```

---

# 10. Verify a Newly Created User

After creating a user, check:

### User information

```bash
id testuser
```

### `/etc/passwd`

```bash
grep "^testuser:" /etc/passwd
```

### Home directory

```bash
ls -ld /home/testuser
```

### Default files

```bash
ls -la /home/testuser
```

### Login shell

```bash
getent passwd testuser
```

---

# 11. 4-Year Interview Questions

### Q1. What is the difference between `useradd` and `adduser`?

`useradd` is the lower-level Linux user creation command. `adduser` is generally a more interactive/user-friendly utility, particularly on Debian-based systems.

---

### Q2. How do you create a user with a home directory?

```bash
sudo useradd -m username
```

---

### Q3. How do you create a user with a specific UID?

```bash
sudo useradd -u 1500 username
```

---

### Q4. How do you create a user with `/bin/bash` as the shell?

```bash
sudo useradd -m -s /bin/bash username
```

---

### Q5. How do you create a system user?

```bash
sudo useradd -r username
```

---

### Q6. Why would you use `/sbin/nologin` for a service account?

To prevent interactive login while still allowing the account to be used by a service when required.

---

### Q7. What is `/etc/skel`?

`/etc/skel` contains default files that are copied into a new user's home directory when the home directory is created.

---

### Q8. How do you verify that a user was created successfully?

```bash
id username
```

```bash
getent passwd username
```

```bash
ls -ld /home/username
```

---

### Q9. What is the difference between `-m` and `-M`?

```text
-m → Create home directory
-M → Do not create home directory
```

---

### Q10. How would you create a service user that cannot log in interactively?

```bash
sudo useradd -r -s /usr/sbin/nologin appuser
```

---

# Practical Commands

```bash
# Create user
sudo useradd testuser

# Create user with home directory
sudo useradd -m testuser

# Create user with bash shell
sudo useradd -m -s /bin/bash testuser

# Create user with specific UID
sudo useradd -m -u 1500 testuser

# Create user with comment
sudo useradd -m -c "Linux Test User" testuser

# Create system user
sudo useradd -r appuser

# Create service user with no interactive login
sudo useradd -r -s /usr/sbin/nologin appuser

# Verify user
id testuser

# Check passwd entry
getent passwd testuser

# Check home directory
ls -ld /home/testuser

# Check skeleton files
ls -la /etc/skel


