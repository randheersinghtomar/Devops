
My index
# Amazon S3 – Short & Complete Index

## 1. S3 Basics
- Introduction to S3
- Bucket & Object
- Object Key, URI & URL
- Bucket Naming & Regions

## 2. Storage & Object Management
- S3 Storage Classes
- Upload, Download, Copy & Delete
- Multipart Upload
- Object Metadata & Tags

## 3. Versioning & Lifecycle

- S3 Versioning
- Version ID & Delete Marker
- Object Recovery
- S3 Lifecycle Policy
  - Lifecycle Rules
  - Transitions
  - Object Expiration

## 4. S3 Security
- IAM Policy vs Bucket Policy
- Block Public Access
- ACL & Object Ownership
- S3 Encryption (SSE-S3, SSE-KMS, SSE-C)
- Cross-Account Access
- IAM Access Analyzer — Basics

## 5. S3 Advanced Features
- Replication (SRR & CRR)
- Pre-Signed URLs
- Static Website Hosting
- Event Notifications
- Object Lock
- Access Points
- Transfer Acceleration
- S3 Batch Operations
- S3 Storage Lens
- S3 Access Grants
- Directory Buckets — Basics

## 6. Monitoring & Troubleshooting
- CloudWatch
- CloudTrail
- Access Logging
- Common S3 Access/Permission Issues

## 7. Hands-on & Interview Practice
- S3 using AWS Console & CLI
- Real-world Troubleshooting Scenarios
- Interview Questions

---

# AWS S3 – Day 01

## Topics Covered

- Introduction to Amazon S3 (Done)
- S3 Bucket and Object (Done)
- Object Key, S3 URI and Object URL (Done)
- S3 Storage Classes (Done)
- S3 Versioning (Done)
- Delete Marker and Object Recovery (Done)
- S3 Lifecycle Policy
- Hands-on Practice

---
---
---

# 1. S3 Basics

## 1.1 Introduction to Amazon S3

**Amazon S3 (Simple Storage Service)** is an AWS service used to **store and retrieve data as objects**.

### Key Features

- **Object storage** – stores files as objects.
- **Highly durable** – designed for **99.999999999% (11 9's) durability**.
- **Scalable** – can store a very large amount of data.
- **Accessible** through AWS Console, CLI, APIs, etc.
- Supports **versioning, encryption, lifecycle policies, replication**, etc.

### Common Use Cases

- Backup and restore
- Storing application files
- Log storage
- Static website hosting
- Data lakes
- Disaster recovery

### Interview Answer

> Amazon S3 is an object storage service provided by AWS. It stores data as objects inside buckets. We commonly use S3 for backups, logs, images, documents, static files and archival data. S3 provides high durability and scalable storage.
---

## 1.2 S3 Bucket & Object

Think of an S3 **Bucket as a container** and an **Object as a file inside that container**.

```text
AWS Account
    |
    └── S3 Bucket
          |
          ├── file1.txt       ← Object
          ├── image.jpg       ← Object
          └── backup.zip      ← Object
````

### Bucket

A **bucket** is a container used to store objects.

* Bucket name must be **globally unique**.
* A bucket is created in a specific **AWS Region**.
* Bucket access can be controlled using policies.

### Object

An **object** is the actual data stored in S3.

An object mainly consists of:

```text
Object
 ├── Data
 ├── Key
 └── Metadata
```

Example:

```text
Bucket: my-company-backup

Object:
my-company-backup/database/backup.sql
```
### Interview Answer

> An S3 bucket is a container where we store objects. An object is the actual data such as images, documents, logs or backup files stored inside the bucket.

---

## 1.3 Object Key, S3 URI & Object URL

### Object Key

The **object key is the complete name/path used to identify an object inside a bucket.**

Object Key — "What is the file called?"

Example:

```text
Bucket: my-bucket

Key: backups/database/backup.sql
```

> S3 does **not actually use folders** like a Linux filesystem. `backups/database/` is part of the object's key.

### S3 URI

Used to identify an S3 object in AWS.

S3 URI — "Where is the file in S3?"

```text
s3://bucket-name/object-key
```

Example:

```text
s3://my-bucket/backups/database/backup.sql
```

### Object URL

Used to access an object through an HTTP/HTTPS URL, depending on the object's permissions.

Example:

```text
https://my-bucket.s3.amazonaws.com/backups/database/backup.sql
```
Key points:- 
Having an Object URL does **not** mean that the object is publicly accessible.
If public access is blocked and the user does not have permission, accessing the Object URL can return:

```text
Access Denied
```

### Easy Difference

Imagine a **hotel**:

```text
Hotel       → Bucket
Room number → Object Key
Full address → S3 URI
Door access/link → Object URL

Object Key → backups/database/backup.sql
S3 URI     → s3://my-bucket/backups/database/backup.sql
Object URL → https://my-bucket.s3.amazonaws.com/backups/database/backup.sql
```
```
```

---

## 1.4 Bucket Naming & Regions

### Bucket Naming

S3 bucket names must:

* Be **globally unique** across S3.
* Generally be **3–63 characters** long.
* Use lowercase letters, numbers, hyphens, and periods where allowed.
* Not contain spaces or underscores.

Example:

```text
my-company-backup-2026
```

### S3 Region

A bucket is created in a specific AWS Region.

Example:

```text
S3 Bucket
    |
    └── Region: ap-south-1 (Mumbai)
```

The region affects:

* Data access latency
* Data transfer costs
* Compliance/data residency
* Integration with other AWS services

> **Interview Point:** S3 is a **regional service**, but bucket names are **globally unique**.

```
```
---
---

# 2. Storage & Object Management

## 2.1 S3 Storage Classes

**S3 Storage Classes** are different options for storing data based on **how often you access it and how much you want to pay**.

S3 provides different storage classes based on:

- Access frequency
- Retrieval requirement
- Availability requirement
- Storage cost
- Archival requirement

Think of them like different types of storage rooms:

```text
Frequently accessed
        ↓
   S3 Standard
        ↓
Less frequently accessed
        ↓
   Standard-IA
        ↓
Rarely accessed
        ↓
      Glacier
        ↓
Almost never accessed
        ↓
  Glacier Deep Archive
````

### Important Storage Classes

| Storage Class                     | Best For                                         |
| --------------------------------- | ------------------------------------------------ |
| **S3 Standard**                   | Frequently accessed data                         |
| **S3 Intelligent-Tiering**        | Data with changing/unknown access patterns       |
| **S3 Standard-IA**                | Data accessed less frequently                    |
| **S3 One Zone-IA**                | Infrequently accessed data that can be recreated |
| **S3 Glacier Instant Retrieval**  | Archive data that needs quick access             |
| **S3 Glacier Flexible Retrieval** | Long-term archive data                           |
| **S3 Glacier Deep Archive**       | Very rarely accessed, long-term archive          |



### 6.1 S3 Standard— "I use it regularly"

Used for **frequently accessed data**.

Features:

- Multi-AZ
- High availability
- Millisecond access
- 99.999999999% (11 9s) durability
- Suitable for frequently accessed objects

Examples:

- Active application files
- Frequently accessed documents
- Images
- Website content

#### Interview Answer

> S3 Standard is used for frequently accessed data. It provides high availability, Multi-AZ storage and 11 9s durability.

### 6.2 S3 Intelligent-Tiering— "I don't know how often I'll use it"

Used when the access pattern of data is **unknown or changes over time**.

S3 monitors access patterns and automatically moves objects between appropriate access tiers to optimize storage cost.

Features:

- Automatic cost optimization
- Useful when access pattern is unpredictable
- Monitoring and automation charges apply

#### Interview Answer

> S3 Intelligent-Tiering is useful when we don't know how frequently data will be accessed. AWS automatically moves objects between access tiers based on usage to optimize storage cost.

### 6.3 S3 Standard-IA— "I rarely use it, but when I need it, I need it immediately"

IA stands for **Infrequent Access**.

Used when data is accessed infrequently but must be available immediately when required.

Features:

- Multi-AZ
- Millisecond retrieval
- Lower storage cost than S3 Standard
- Retrieval charges apply
- Minimum storage duration: 30 days

Examples:

- Backups
- Disaster recovery files
- Infrequently accessed business data

#### Interview Answer

> S3 Standard-IA is used for data that is accessed infrequently but needs immediate retrieval when required. Storage cost is lower than S3 Standard, but retrieval charges apply.

### 6.4 S3 One Zone-IA— "I rarely use it and I can recreate it"

Used for infrequently accessed data stored in a **single Availability Zone**.

Features:

- Single AZ
- Immediate retrieval
- Lower cost than Standard-IA
- Retrieval charges apply
- Minimum storage duration: 30 days
- Suitable for re-creatable data

Because the data is stored in one AZ, it should not normally be used as the only copy of critical data.

Recreate = "If I lose this file, I can make the same data again from another source."

#### Interview Answer

> S3 One Zone-IA is used for infrequently accessed and re-creatable data. It stores data in a single Availability Zone and costs less than Standard-IA.

---

## 7. S3 Glacier Storage Classes

Glacier storage classes are mainly used for **archive data**.


### 7.1 Glacier Instant Retrieval

Used for archive data that is rarely accessed but needs **millisecond retrieval** when required.

```text
Archive Data
+
Rarely Accessed
+
Millisecond Retrieval
```

### 7.2 Glacier Flexible Retrieval

Used for archive data where immediate retrieval is not required.

Retrieval can range from **minutes to hours**, depending on the retrieval option.

```text
Archive Data
+
Rarely Accessed
+
Minutes/Hours Retrieval
```

### 7.3 Glacier Deep Archive

Used for **long-term archival data** that is very rarely accessed.

Examples:

- Long-term compliance records
- Old financial records
- Historical backups
- Long-term company records

Retrieval normally takes hours.

```text
Long-Term Archive
+
Very Rare Access
+
Hours Retrieval
+
Very Low Storage Cost
```

#### Easy Interview Point

> **S3 Standard = frequently used data, IA = occasionally used data, Glacier = archive data.**

---

# 2.2 Upload, Download, Copy & Delete

S3 allows us to perform basic operations on objects.

```text
Bucket
  |
  ├── Upload    → Put file into S3
  ├── Download  → Get file from S3
  ├── Copy      → Make a copy of an object
  └── Delete    → Remove an object
```

### Upload

Putting a file into an S3 bucket.

```text
Laptop → S3 Bucket
```

Example:

```bash
aws s3 cp backup.zip s3://my-bucket/
```

### Download

Getting an object from S3 to your system.

```text
S3 Bucket → Laptop
```

```bash
aws s3 cp s3://my-bucket/backup.zip .
```

### Copy

Copying an object from one S3 location to another.

```text
Bucket A → Bucket B
```

```bash
aws s3 cp s3://bucket-a/file.txt s3://bucket-b/
```

### Delete

Removing an object from a bucket.

```bash
aws s3 rm s3://my-bucket/file.txt
```

> **Important:** If **Versioning is enabled**, deleting an object normally creates a **Delete Marker** instead of immediately permanently removing all versions.

---

# 2.3 Multipart Upload

**Multipart Upload** means breaking a large file into **smaller parts** and uploading those parts separately.

Instead of:

```text
10 GB File
    |
    ↓
Single Upload
```

S3 can do:

```text
10 GB File
    |
    ├── Part 1
    ├── Part 2
    ├── Part 3
    ├── Part 4
    └── ...
          ↓
       S3 Bucket
          ↓
    S3 combines parts
```

### Why use it?

* Better for **large files**
* Parts can be uploaded **in parallel**
* If one part fails, we can retry **only that part**
* Improves upload performance

> **Interview Point:** Multipart Upload is especially useful for large objects and unreliable network connections.

---

# 2.4 Object Metadata & Tags

## Object Metadata

**Metadata is information about the object.**

Think:

> **File = actual data**
> **Metadata = information about the file**

Example:

```text
Object: backup.zip

Metadata:
├── Content-Type
├── Size
├── Last Modified
└── Encryption information
```

Some metadata is automatically created by S3, while some can be provided by the user/application.

## Object Tags

**Tags are key-value labels attached to an S3 object.**

Example:

```text
Object: backup.zip

Tags:
Environment = Production
Department  = Finance
Backup      = Daily
```

Tags are useful for:

* Organizing objects
* Lifecycle rules
* Cost management
* Identifying data
* Automation

### Metadata vs Tags

```text
Metadata → Information/details about the object

Tags     → Labels we attach to the object
```

### Easy Example

Imagine a **parcel**:

```text
Parcel
  |
  ├── Actual item     → Object
  ├── Parcel details  → Metadata
  └── Labels          → Tags
```

### Interview Answer

> "S3 storage classes help us choose the appropriate storage based on access frequency and cost. We can upload, download, copy, and delete objects. Multipart upload breaks large files into smaller parts for efficient and reliable uploads. Metadata provides information about an object, while tags are key-value labels used for organization and management."

```
```
---
---

# 3. S3 Versioning & Lifecycle

## 3.1) S3 Versioning

**S3 Versioning** keeps multiple versions of the same object in a bucket.

Without Versioning:

```text
data.txt
   ↓
Upload new data.txt
   ↓
Old file is replaced
````

With Versioning:

```text
data.txt
   |
   ├── Version 1 → Old content
   ├── Version 2 → Updated content
   └── Version 3 → Latest content
```

### Why use Versioning?

* Protects against accidental deletion
* Protects against accidental overwriting
* Allows recovery of previous versions
* Useful for data protection and backup

> **Important:** Versioning must be enabled on the bucket.

## Versioning Hands-on

The following practical was completed:

### Step 1

Created S3 bucket:

```text
Randheer-s3-practice-2026
```

### Step 2

Uploaded:

```text
Docker.docx
```

### Step 3

Enabled S3 Versioning.

### Step 4

Uploaded the same object again.

Verified multiple versions:

```text
Docker.docx → New Version ID
Docker.docx → Previous Version
```

The original object uploaded before enabling Versioning had:

```text
Version ID = null
```

### Step 5

Deleted the current object.

Verified:

```text
Delete Marker
```

was created.

### Step 6

Previous object versions remained available.

### Step 7

Deleted the Delete Marker.

The previous latest version became visible again.

### Practical Result

```text
Versioning Enabled
        ↓
Same Object Uploaded
        ↓
Multiple Versions Created
        ↓
Object Deleted
        ↓
Delete Marker Created
        ↓
Delete Marker Removed
        ↓
Object Restored
```

## 3.2) Version ID & Delete Marker

### Version ID

Every version of an object gets a **unique Version ID**.

Example:

```text
data.txt

Version ID: abc123 → Old version
Version ID: xyz789 → New version
```

The latest version is normally returned when you request the object without specifying a Version ID.

### Delete Marker

When **Versioning is enabled**, deleting an object does not immediately remove the previous version.

Instead, S3 creates a **Delete Marker**.

```text
data.txt
   |
   ├── Version 1 → File
   ├── Version 2 → File
   └── Delete Marker
```

To the normal user, the object appears deleted.

But the previous versions still exist.

> **Simple meaning:** Delete Marker = Makes the object look deleted while keeping previous versions.

## 3.3) Object Recovery

Versioning allows you to recover accidentally deleted or overwritten objects.

### Accidental Deletion

Before deletion:

```text
data.txt
   ↓
Version 1
```

After deletion:

```text
data.txt
   ↓
Delete Marker
```

The old version still exists.

To recover the object, remove the **Delete Marker**.

```text
Delete Marker ❌
Version 1      ✅
       ↓
data.txt is recovered
```

### Accidental Overwrite

If you accidentally upload incorrect content:

```text
Version 1 → Correct file
Version 2 → Wrong file
```

You can retrieve the previous version.


# 3.4) Lifecycle Policy

**S3 Lifecycle** automatically manages objects based on their age.

Lifecycle Policy Example: 

Suppose application logs are uploaded to S3.

Requirement:

```text
Day 1 - 30
S3 Standard

Day 31 - 90
Standard-IA

Day 91 - 180
Glacier Flexible Retrieval

Day 181+
Glacier Deep Archive

Then Delete ( If required)
```

Lifecycle configuration:

```text
30 Days  → Standard-IA
90 Days  → Glacier Flexible
180 Days → Glacier Deep Archive
Delete
```

Important:

The number of days refers to the **age of the object from its creation/upload**, not the number of days spent in the previous storage class.



### Lifecycle Transition

A **Transition** means moving an object from one S3 storage class to another.

Example:

```text
S3 Standard
     ↓ 30 days
S3 Standard-IA
     ↓ 60 days
S3 Glacier
```

This can reduce storage costs for older or rarely accessed data.

### Example

> Move objects from S3 Standard to S3 Glacier after 90 days.

S3 automatically performs the transition according to the lifecycle rule.


### Object Expiration

**Expiration** means S3 automatically deletes objects after a specified period.

Example:

```text
Object uploaded
      ↓
180 days
      ↓
Object expires
      ↓
Deleted
```

### Example

For temporary application logs:

```text
logs/
   ↓
Keep for 30 days
   ↓
Automatically delete
```

You can create a lifecycle rule:

> Delete objects under `logs/` after 30 days.


# 3.5) Lifecycle with Versioning

Versioning can create multiple old versions of objects.

These old versions consume storage.

Lifecycle rules can manage:

- Current object versions
- Noncurrent object versions
- Delete markers
- Incomplete multipart uploads

For example:

```text
Old Version
    ↓
Transition to cheaper storage
    ↓
Expire after retention period
```

This helps reduce unnecessary S3 storage cost.



# Quick Interview Revision

| Term                | Simple Meaning                                                |
| ------------------- | ------------------------------------------------------------- |
| **Versioning**      | Keeps multiple versions of an object                          |
| **Version ID**      | Unique ID for each object version                             |
| **Delete Marker**   | Makes an object appear deleted while previous versions remain |
| **Object Recovery** | Recover deleted/overwritten objects using previous versions   |
| **Lifecycle Rule**  | Automatically manages objects based on age                    |
| **Transition**      | Moves objects to another storage class                        |
| **Expiration**      | Automatically deletes objects after a specified time          |


## Easy Way to Remember

```text
VERSIONING
    ↓
Keep old versions
    ↓
Recover deleted/overwritten data


LIFECYCLE
    ↓
Automate object management
    ↓
Transition → Move to cheaper storage
Expiration → Delete object
```


---
---
---

# 15. Lifecycle Policy Hands-on

Lifecycle configuration was reviewed from the AWS Console.

Path:

```text
S3
→ Bucket
→ Management
→ Lifecycle Rules
→ Create Lifecycle Rule
```

Lifecycle actions reviewed:

- Transition current versions
- Transition noncurrent versions
- Expire current versions
- Permanently delete noncurrent versions
- Delete expired delete markers
- Delete incomplete multipart uploads

Example transition configuration discussed:

```text
Standard
   ↓ Day 30
Standard-IA
   ↓ Day 90
Glacier Flexible
   ↓ Day 180
Glacier Deep Archive
```

The rule was reviewed for hands-on understanding without waiting for the actual transition period.

---

# 16. Day 01 Important Interview Questions

### Q1. What is Amazon S3?

Amazon S3 is an object storage service used to store data as objects inside buckets.

### Q2. What is the difference between a bucket and an object?

A bucket is a container, while an object is the actual data stored inside the bucket.

### Q3. What is an Object Key?

An Object Key uniquely identifies an object inside an S3 bucket.

### Q4. Which storage class is suitable for frequently accessed data?

S3 Standard.

### Q5. Which class is suitable for infrequently accessed data requiring immediate retrieval?

S3 Standard-IA.

### Q6. Standard-IA vs One Zone-IA?

Standard-IA stores data across multiple AZs, while One Zone-IA stores data in a single AZ and costs less.

### Q7. When would you use Intelligent-Tiering?

When the access pattern is unknown or changes over time.

### Q8. What is S3 Versioning?

Versioning maintains multiple versions of the same object and helps recover from accidental overwrite or deletion.

### Q9. What happens when we delete an object from a version-enabled bucket?

S3 normally creates a Delete Marker while previous versions remain available.

### Q10. What is an S3 Lifecycle Policy?

It automatically transitions objects between storage classes or expires them based on configured rules.

### Q11. Why do we use Lifecycle Policies?

To automate data management and optimize S3 storage costs.

---
# AWS S3 — Day 02


---

# 1. IAM Policy vs Bucket Policy

Both can be used to control access to S3 resources.

### IAM Policy

IAM policy is attached to an IAM identity such as a user or role.

Example:

Amit needs access to `company-data`.

We can attach an IAM policy to Amit allowing access to:

arn:aws:s3:::company-data

### Bucket Policy

Bucket policy is attached directly to the S3 bucket.

Example:

The `company-data` bucket can allow a specific IAM user or role using its ARN.

### Easy Difference

IAM Policy:
User/Role → Permission → S3

Bucket Policy:
S3 Bucket → Permission → User/Role

### When to Use

- IAM Policy → Manage permissions from the identity side.
- Bucket Policy → Manage permissions from the bucket/resource side.
- Bucket Policy is commonly used for cross-account S3 access.

---

# 2. Access Control List (ACL)

ACL is a legacy method of controlling access to S3 buckets and objects.

Generally, ACLs are kept disabled and access is managed using IAM policies and bucket policies.

Interview Answer:

> ACL is a legacy S3 access-control mechanism. In modern S3 environments, we generally keep ACLs disabled and use policies for access management.

---

# 3. S3 Encryption

## Encryption at Rest

Data stored inside S3 is encrypted.

## Encryption in Transit

Data travelling between the client and S3 is protected using HTTPS/TLS.

---

## SSE-S3

S3 manages the encryption keys.

- Simple
- AWS/S3 manages keys
- New S3 object uploads are encrypted at rest by default.

Interview:

> SSE-S3 is server-side encryption where Amazon S3 manages the encryption keys.

---

## SSE-KMS

Uses AWS Key Management Service (KMS).

Provides more control and auditing compared with SSE-S3.

KMS keys can be:

- AWS Managed Key
- Customer Managed Key

Customer managed keys provide more control over permissions and key management.

Interview:

> SSE-KMS uses AWS KMS for encryption and provides additional control and auditing capabilities.

---

## SSE-C

Customer provides and manages the encryption key.

S3 uses the provided key for encryption/decryption but does not store the encryption key itself.

Interview:

> In SSE-C, the customer provides and manages the encryption key.

---

## S3 Bucket Key

Used with SSE-KMS.

Purpose:

S3 Bucket Key
→ Fewer direct KMS requests
→ Helps reduce KMS request cost

It is enabled from the S3 bucket encryption settings.

---

# 4. S3 Static Website Hosting

S3 can host static websites containing files such as:

- HTML
- CSS
- JavaScript
- Images

Basic flow:

Create Bucket
→ Upload Website Files
→ Enable Static Website Hosting
→ Configure index.html
→ Configure Public Read Access if required
→ Add Bucket Policy
→ Open Website Endpoint

For public object access, commonly required permission:

s3:GetObject

---

# 5. S3 Server Access Logging

Used to record requests made to an S3 bucket.

Example:

Source Bucket:
company-data

Destination Bucket:
company-s3-logs

Flow:

User Request
→ company-data
→ Access Log
→ company-s3-logs

Interview:

> S3 Server Access Logging records requests made to an S3 bucket and delivers the logs to a destination S3 bucket.

---

# 6. S3 Event Notifications

S3 can generate events when actions occur in a bucket.

Examples:

- Object Created
- Object Removed

Events can be sent to:

- Lambda
- SNS
- SQS

Example:

File Upload
→ S3 Object Created Event
→ Lambda

NOTE:

Lambda integration hands-on lab will be completed during the Lambda topic.

---

# 7. S3 Object Lock

Used to protect object versions from deletion or overwrite.

Uses:

WORM = Write Once, Read Many

Requires versioning.

## Governance Mode

Authorized users with special permission can bypass retention.

## Compliance Mode

More strict.

Protected object versions cannot be deleted during the retention period, even by the root user.

Use cases:

- Audit records
- Compliance data
- Financial records

---

# 8. S3 Replication

Automatically replicates objects between S3 buckets.

Versioning must be enabled on both source and destination buckets.

## CRR — Cross-Region Replication

Source and destination buckets are in different AWS Regions.

Common use:

- Disaster Recovery
- Compliance
- Geographical separation

## SRR — Same-Region Replication

Source and destination buckets are in the same Region.

Common use:

- Log aggregation
- Separate account/team ownership
- Same-region copy requirements

---

# 9. CORS

CORS = Cross-Origin Resource Sharing.

Used when a web application from one origin needs to access S3 resources from another origin through the browser.

Example:

Website:
www.example.com

Image:
S3 Bucket

Website → Browser → S3 Image

If required, S3 CORS rules allow the website origin and HTTP methods.

Important:

CORS does NOT make an object public.

---

# 10. Presigned URL

Provides temporary access to private S3 resources without making them public.

Example:

Private File:
salary-report.pdf

Generate Presigned URL
→ Valid for limited time
→ User accesses file
→ URL expires

Presigned URLs can be used for operations such as temporary download and, when generated appropriately, upload.

Interview:

> A presigned URL provides time-limited access to a private S3 resource without making the bucket public.

---

# 11. Multipart Upload

Used to upload large objects in multiple parts.

Example:

10 GB File
→ Part 1
→ Part 2
→ Part 3
→ Part 4
→ ...

Multiple parts can be uploaded concurrently.

If one part fails:

Part 1 → Success
Part 2 → Failed → Retry
Part 3 → Can continue
Part 4 → Can continue

Benefits:

- Better performance
- Parallel/concurrent uploads
- Individual failed parts can be retried
- Efficient for large files

AWS recommends considering multipart upload for objects of 100 MB or larger.

Example CLI:

aws s3 cp backup.zip s3://company-data/

AWS CLI can automatically use multipart upload for large transfers based on its transfer configuration.

### Incomplete Multipart Upload

If a multipart upload is never completed, uploaded parts may remain and incur storage cost.

Lifecycle policies can be configured to automatically abort incomplete multipart uploads after a specified number of days.

---

# 12. S3 Transfer Acceleration

Improves long-distance transfers between users and an S3 bucket.

Example:

US User
→ Nearest AWS Edge Location
→ AWS Global Network
→ S3 Mumbai

Useful when users are geographically far from the bucket.

Additional charges apply.

Interview:

> S3 Transfer Acceleration uses AWS edge locations and the AWS global network to improve long-distance transfers to and from S3.

---

# 13. S3 Storage Lens

Provides centralized visibility and analytics for S3 storage.

Can help analyse:

- Storage usage
- Object count
- Usage trends
- Activity metrics

Useful when an organisation has many S3 buckets/accounts.

Console:

S3
→ Storage Lens
→ Dashboards

Interview:

> S3 Storage Lens provides centralized visibility and analytics for S3 storage usage and activity.

---

# 14. S3 Access Points

Basic awareness only.

S3 Access Points can simplify access management when multiple applications or teams use a shared bucket.

Example:

company-data
├── HR Access Point
├── Finance Access Point
└── Application Access Point

Each access point can have its own access policy.

---

# 15. S3 Cross-Account Access — EC2 Role + Bucket Policy

## Scenario

Account A:
- EC2 Instance
- IAM Role for EC2

Account B:
- S3 Bucket = `companydata`

Requirement:

EC2 in Account A needs to access the S3 bucket in Account B.

We do not want to configure Access Key and Secret Key on EC2.

---

## Step 1 — Create IAM Role in Account A

Go to:

IAM → Roles → Create Role

Trusted Service:

EC2

Attach the required S3 permissions.

Example:

- s3:ListBucket
- s3:GetObject
- s3:PutObject

Example Role Name:

EC2-S3-Role

---

## Step 2 — Attach Role to EC2

Go to:

EC2 → Instance → Actions → Security → Modify IAM Role

Attach:

EC2-S3-Role

Now EC2 can use temporary credentials from the IAM Role.

No `aws configure` is required.

---

## Step 3 — Copy EC2 Role ARN

Go to:

IAM → Roles → EC2-S3-Role

Copy the Role ARN.

Example:

arn:aws:iam::111111111111:role/EC2-S3-Role

---

## Step 4 — Allow the Role in Account B Bucket Policy

Login to Account B.

Go to:

S3 → companydata → Permissions → Bucket Policy

In the bucket policy:

Principal:
Account A's EC2 Role ARN

Actions:
- s3:ListBucket
- s3:GetObject
- s3:PutObject

Resources:
- arn:aws:s3:::companydata
- arn:aws:s3:::companydata/*

### Flow

Account A

EC2
  ↓
EC2-S3-Role
  ↓
Role ARN allowed in Account B Bucket Policy
  ↓
Account B
companydata

---

## Step 5 — Test from EC2

Login to EC2 and run:

```bash
aws s3 ls s3://companydata
```
---
# 16. IAM Cross-Account Access — Using Assume Role / Switch Role

### Scenario

Account A:
- IAM User = Amit

Account B:
- Resources that Amit needs to access

Requirement:

Amit needs to switch from Account A and temporarily use permissions from Account B.

### Step 1 — Create Role in Account B

Go to:

IAM → Roles → Create Role

Select:

Trusted Account = Account A

Enter:

Account A's Account ID

Example Role Name:

cross

Attach the required permissions to the role.

Example:

If Amit needs S3 access:

AmazonS3ReadOnlyAccess
or required custom S3 permissions

### Step 2 — Copy Account B Role ARN

Example:

arn:aws:iam::222222222222:role/cross

### Step 3 — Give Amit AssumeRole Permission in Account A

In Account A, create a policy for Amit:

Action:

sts:AssumeRole

Resource:

arn:aws:iam::222222222222:role/cross

Example:

{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Action": "sts:AssumeRole",
      "Resource": "arn:aws:iam::222222222222:role/cross"
    }
  ]
}

Attach this policy to Amit.

### Step 4 — Switch Role

Login as Amit in Account A.

Go to:

Switch Role

Enter:

Account ID = Account B's Account ID
Role Name  = cross

Then switch the role.

```

```

# Amazon S3 Mount on EC2 using IAM Role (s3fs)

## Objective

Mount an Amazon S3 bucket on an EC2 instance as a Linux file system using **s3fs** and **IAM Role** without storing AWS Access Keys.

I created an S3 bucket, attached an IAM Role with S3 permissions to my EC2 instance, installed s3fs, created a mount point, mounted the bucket using the correct regional endpoint, and verified it by creating a test file that appeared in the S3 bucket. I used IAM Role-based authentication instead of Access Keys as a security best practice.


---

# Architecture

```
                   +----------------------+
                   |      IAM Role        |
                   |   S3MountRole        |
                   +----------+-----------+
                              |
                              | Temporary Credentials
                              |
                              ▼
+------------------------------------------------------+
|                    Amazon EC2                        |
|------------------------------------------------------|
| Ubuntu                                               |
| s3fs                                                 |
| Mount Point : /mnt/s3bucket                          |
+-------------------------+----------------------------+
                          |
                          | HTTPS
                          ▼
               +--------------------------+
               | Amazon S3 Bucket         |
               | imtiyaj-s3-practice-2026 |
               +--------------------------+
```

---

# Prerequisites

- AWS Account
- EC2 Instance (Ubuntu)
- Amazon S3 Bucket
- IAM Role
- Internet Connectivity
- sudo privileges

---

# Step 1 – Create S3 Bucket

Bucket Name

```
imtiyaj-s3-practice-2026
```

Region

```
ap-south-1 (Mumbai)
```

---

# Step 2 – Create IAM Role

Role Name

```
S3MountRole
```

Policy Attached

```
AmazonS3FullAccess
```

---

# Step 3 – Attach IAM Role to EC2

EC2

→ Actions

→ Security

→ Modify IAM Role

→ Select

```
S3MountRole
```

---

# Step 4 – Verify IAM Role

```bash
curl http://169.254.169.254/latest/meta-data/iam/security-credentials/
```

Expected Output

```
S3MountRole
```

Purpose

- Verify IAM Role is attached
- Verify EC2 can access Metadata Service

---

# Step 5 – Install s3fs

```bash
sudo apt update

sudo apt install s3fs -y
```

Verify

```bash
s3fs --version
```

---

# Step 6 – Create Mount Point

```bash
sudo mkdir /mnt/s3bucket
```

Verify

```bash
ls -ld /mnt/s3bucket
```

---

# Step 7 – Mount S3 Bucket

```bash
sudo s3fs imtiyaj-s3-practice-2026 /mnt/s3bucket \
-o iam_role=auto \
-o allow_other \
-o use_path_request_style \
-o url=https://s3-ap-south-1.amazonaws.com \
-o endpoint=ap-south-1
```

### Command Explanation

| Option | Description |
|---------|-------------|
| iam_role=auto | Uses IAM Role credentials automatically |
| allow_other | Allows other Linux users to access mount |
| use_path_request_style | Uses path-style bucket requests |
| url | Specifies S3 regional endpoint |
| endpoint | Uses correct AWS signing region |

---

# Step 8 – Verify Mount

```bash
df -h
```

Expected

```
s3fs    64P    0   64P    0%   /mnt/s3bucket
```

---

# Step 9 – List Bucket

```bash
ls -la /mnt/s3bucket
```

---

# Step 10 – Upload File

```bash
echo "S3 mount test" | sudo tee /mnt/s3bucket/test.txt
```

Verify

```bash
ls -l /mnt/s3bucket
```

---

# Step 11 – Verify in AWS Console

Open

```
Amazon S3

↓

imtiyaj-s3-practice-2026

↓

test.txt
```

File should be visible.

---

# Step 12 – Delete File

```bash
sudo rm /mnt/s3bucket/test.txt
```

Verify from AWS Console.

---

# How It Works

```
Application

↓

Linux Filesystem

↓

s3fs (FUSE)

↓

HTTPS

↓

Amazon S3
```

Applications think they are writing to a local directory.

Actually, data is stored inside Amazon S3.

---

# Advantages

- No Access Keys stored on EC2
- Uses Temporary IAM Credentials
- Secure Authentication
- Easy File Access
- Cost Effective
- Production Ready

---

# Limitations

- S3 is Object Storage (Not Block Storage)
- Higher latency than EBS
- Not suitable for databases
- File locking is limited
- Depends on network connectivity

---

# Troubleshooting

## Access Denied

Verify IAM Role has S3 permissions.

---

## AuthorizationHeaderMalformed

Reason

Wrong AWS Region

Solution

```
-o endpoint=ap-south-1

-o url=https://s3-ap-south-1.amazonaws.com
```

---

## Mount Failed

Check

```bash
df -h
```

Check logs

```bash
sudo s3fs \
-o dbglevel=info \
-f
```

---

## Bucket Empty

Verify

```bash
aws s3 ls s3://imtiyaj-s3-practice-2026
```

or

Check S3 Console.

---

# Production Best Practices

- Use IAM Roles instead of Access Keys
- Apply Least Privilege Principle
- Enable Bucket Versioning
- Enable Server Side Encryption
- Enable Bucket Logging
- Enable Lifecycle Policies
- Restrict Bucket Access using Bucket Policies
- Monitor using CloudTrail
```
---
```
# Interview Questions

### Why use IAM Role instead of Access Keys?

IAM Roles provide temporary credentials through the EC2 Metadata Service, eliminating the need to store long-term AWS Access Keys.

---

### Why use s3fs?

It allows an Amazon S3 bucket to be mounted as a Linux filesystem using FUSE.

---

### Why create /mnt/s3bucket?

Linux requires a mount point where the remote filesystem is attached.

---

### Why specify endpoint?

To ensure requests are signed for the correct AWS Region.

---

### Can S3 replace EBS?

No.

S3 is Object Storage.

EBS is Block Storage.

---

### Can databases run on S3 Mount?

No.

Databases require low latency and block storage.

---

### Is this Production Ready?

Yes.

Many organizations use IAM Role + s3fs for storing logs, reports, backups, static files, and application data.

---

# Skills Demonstrated

- Amazon S3
- EC2
- IAM Role
- Metadata Service (IMDSv2)
- Linux
- s3fs
- FUSE
- Mounting File Systems
- Secure Authentication
- AWS Networking
- Production Troubleshooting

---

# Conclusion

Successfully mounted an Amazon S3 bucket on an EC2 instance using **IAM Role-based authentication** and **s3fs**, allowing secure file operations without storing AWS credentials on the server.

```
---

# Production Scenario Questions & Answers

## Q1. A user is getting Access Denied while accessing an S3 object. How will you troubleshoot?

Answer:

First I will confirm which IAM user or role is making the request.

Then I will check:

1. IAM policy
2. S3 bucket policy
3. Any explicit Deny
4. Block Public Access if public access is expected
5. KMS permissions if the object uses SSE-KMS
6. Object/bucket path and required S3 action

An explicit Deny overrides an Allow.


## Q2. An important S3 file was accidentally deleted. How will you recover it?

Answer:

I will first check whether S3 Versioning is enabled.

If versioning is enabled, deletion normally creates a delete marker while previous object versions remain available.

I can restore the required previous version or remove the delete marker as appropriate.

---

## Q3. The company wants old S3 data automatically moved to cheaper storage. What will you do?

Answer:

I will configure an S3 Lifecycle Policy.

Example:

Frequently accessed data
→ S3 Standard

After required number of days
→ Standard-IA

Later
→ Glacier storage class

Finally, if required
→ Expire/Delete

This reduces storage cost automatically.

---

## Q4. A private S3 file needs to be shared with an external user for a limited time. What will you do?

Answer:

I will generate a Presigned URL.

This provides temporary access to the private object without making the bucket public.

The URL expires after the configured period.

---

## Q5. The application uploads very large files to S3 and uploads are slow or individual transfers sometimes fail. What will you recommend?

Answer:

I will use Multipart Upload.

Large files are uploaded in multiple parts and parts can be transferred concurrently.

If an individual part fails during the multipart operation, that part can be retried without restarting all successfully uploaded parts within that active transfer.

---

## Q6. Incomplete multipart uploads are increasing S3 storage cost. How will you fix it?

Answer:

I will configure an S3 Lifecycle rule to abort incomplete multipart uploads after a specified number of days.

This automatically cleans up unused multipart parts and helps avoid unnecessary storage cost.

---

## Q7. Users in another country are experiencing slow uploads to an S3 bucket located far away. What can you use?

Answer:

I will evaluate S3 Transfer Acceleration.

It uses nearby AWS edge locations and the AWS global network to improve long-distance data transfer.

Since it has additional cost, I would enable it when the performance benefit justifies it.

---

## Q8. The company requires audit records that must not be deleted for 7 years. What will you use?

Answer:

I will use S3 Object Lock with an appropriate retention period.

For strict regulatory requirements, Compliance Mode can be used.

It follows the WORM model — Write Once, Read Many.

---

## Q9. The company requires an S3 copy in another AWS Region for Disaster Recovery. What will you configure?

Answer:

I will configure S3 Cross-Region Replication (CRR).

Versioning must be enabled on the source and destination buckets.

I will configure the replication rule and required IAM permissions.

---

## Q10. An EC2 instance in Account A needs access to an S3 bucket in Account B without storing access keys on the server. How will you configure it?

Answer:

I will create an IAM role for EC2 in Account A and attach the required S3 permissions.

Then I will attach the role to the EC2 instance.

In Account B, I will configure the S3 bucket policy to allow the ARN of the Account A EC2 role.

Then from EC2 I can access the bucket, for example:

aws s3 ls s3://companydata

No long-term access keys need to be stored on the EC2 instance.

---

## Q11. A static website hosted on S3 is not opening publicly. What will you check?

Answer:

I will check:

1. Static website hosting is enabled.
2. Correct index document such as index.html is configured.
3. Required website files exist.
4. Bucket policy allows required public GetObject access.
5. S3 Block Public Access settings are consistent with the intended public website configuration.
6. Correct S3 website endpoint is being used.

---

## Q12. An S3 bucket uses SSE-KMS and a user has S3 permissions but still gets Access Denied while downloading an object. What will you check?

Answer:

I will check both S3 permissions and KMS permissions.

With SSE-KMS, having S3 GetObject permission alone may not be sufficient.

I will verify:

- IAM permissions
- Bucket policy
- KMS key policy
- Required KMS permissions
- Any explicit Deny

The user/role must be authorised to use the KMS key for the required operation.

---

# S3 Day 02 — Final Interview Revision

SSE-S3
→ S3 manages encryption keys

SSE-KMS
→ KMS integration + more control/auditing

SSE-C
→ Customer provides/manages key

Bucket Key
→ Reduce KMS request cost

Presigned URL
→ Temporary private access

Multipart Upload
→ Large file + parallel parts + failed-part retry

Transfer Acceleration
→ Long-distance transfer performance

Object Lock
→ WORM / deletion protection

CRR
→ Cross-region replication / DR

Server Access Logging
→ S3 request logs

Storage Lens
→ S3 storage analytics

Cross-Account S3
→ Allow another account's user/role through bucket policy

---

# Pending Hands-On

S3 Event Notification + Lambda integration

This lab will be completed during the AWS Lambda topic.

---
```


